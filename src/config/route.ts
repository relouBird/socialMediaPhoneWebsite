export  const ROUTES = {
    home : "/",
    loader: "/Loader",
    register : "/Auth/Register",
    login : "/Auth/Login",
    verification : "/Auth/Email-verification",
    passwordReset : "/Auth/Password-reset",
    other : "/Other"
}